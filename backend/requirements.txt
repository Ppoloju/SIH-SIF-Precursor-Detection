fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy>=2.0
python-multipart>=0.0.9

# --- Database drivers ---
# psycopg[binary]>=3.2   # PostgreSQL driver (uncomment when DATABASE_URL points at Postgres)

# --- Optional file formats ---
# openpyxl>=3.1           # XLSX ingestion

# --- Optional AI services (imported lazily; the app degrades gracefully) ---
# groq>=0.12            # LLM assistance (Llama via Groq)
# sentence-transformers # semantic embeddings / similar reports
# scikit-learn>=1.5     # traditional ML when labeled data is available